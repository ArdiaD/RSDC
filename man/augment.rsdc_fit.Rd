% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{augment.rsdc_fit}
\alias{augment.rsdc_fit}
\title{Augment data with fitted RSDC regime information}
\usage{
\method{augment}{rsdc_fit}(x, ...)
}
\arguments{
\item{x}{An \code{"rsdc_fit"} object (must carry its estimation residuals).}

\item{...}{Unused; for generic compatibility.}
}
\value{
A data frame with one row per observation: the residual columns
(\code{.resid1}, ...), smoothed regime probabilities
(\code{.smoothed_p1}, ...) and the Viterbi state \code{.state}.
}
\description{
\pkg{broom}-style augmentation: returns the estimation residuals together with
the smoothed regime probabilities and the most likely (Viterbi) regime path.
}
\examples{
\donttest{
y <- scale(matrix(rnorm(200 * 2), 200, 2))
fit <- rsdc_estimate("noX", residuals = y, N = 2)
head(generics::augment(fit))
}
}
