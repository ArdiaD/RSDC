% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R, R/model_estimate.R
\name{print.rsdc_fit}
\alias{print.rsdc_fit}
\alias{coef.rsdc_fit}
\alias{nobs.rsdc_fit}
\alias{logLik.rsdc_fit}
\alias{vcov.rsdc_fit}
\alias{confint.rsdc_fit}
\alias{summary.rsdc_fit}
\alias{predict.rsdc_fit}
\alias{simulate.rsdc_fit}
\alias{rsdc_estimate}
\title{Estimate Regime-Switching or Constant Correlation Model (Wrapper)}
\usage{
\method{print}{rsdc_fit}(x, digits = 4, ...)

\method{coef}{rsdc_fit}(object, ...)

\method{nobs}{rsdc_fit}(object, ...)

\method{logLik}{rsdc_fit}(object, ...)

\method{vcov}{rsdc_fit}(object, ...)

\method{confint}{rsdc_fit}(object, parm, level = 0.95, ...)

\method{summary}{rsdc_fit}(object, ...)

\method{predict}{rsdc_fit}(
  object,
  residuals,
  sigma_matrix,
  value_cols,
  X = NULL,
  out_of_sample = FALSE,
  ...
)

\method{simulate}{rsdc_fit}(object, nsim = 1, seed = NULL, X = NULL, n = NULL, ...)

rsdc_estimate(
  method = c("tvtp", "noX", "const"),
  residuals,
  N = 2,
  X = NULL,
  out_of_sample = FALSE,
  control = list()
)
}
\arguments{
\item{x}{An object of class \code{"rsdc_fit"}.}

\item{digits}{Number of significant digits for printing.}

\item{...}{Further arguments passed to methods (currently unused).}

\item{parm}{Vector of parameter names/indices (default: all).}

\item{level}{Confidence level.}

\item{residuals}{Numeric matrix \eqn{T \times K}. Typically standardized residuals/returns.}

\item{sigma_matrix}{Numeric matrix of conditional standard deviations.}

\item{value_cols}{Columns of \code{sigma_matrix} giving the asset order.}

\item{X}{Numeric matrix \eqn{T \times p} of exogenous covariates (required for \code{"tvtp"}).}

\item{out_of_sample}{Logical. If \code{TRUE}, a fixed 70/30 split is applied prior to estimation.}

\item{nsim}{Unused (kept for generic compatibility; one path is returned).}

\item{seed}{Optional RNG seed.}

\item{n}{Series length for \code{"noX"}/\code{"const"} (defaults to the fitted T).}

\item{method}{Character. One of \code{"tvtp"}, \code{"noX"}, \code{"const"}.}

\item{N}{Integer. Number of regimes. Ignored when \code{method = "const"}.}

\item{control}{Optional list forwarded to the backends and optimisers:
\code{seed} (default 123) and \code{do_trace} (default \code{FALSE}); optimiser
settings \code{itermax}, \code{NP}, \code{parallelType}, \code{steptol} (\pkg{DEoptim})
and \code{maxit} (\code{optim}); and \code{compute_se} (default \code{TRUE}) to
toggle the observed-information standard errors.}
}
\value{
An object of class \code{"rsdc_fit"}: a list with components
\describe{
\item{\code{transition_matrix}}{Estimated transition matrix (\eqn{1 \times 1} for \code{"const"};
for \code{"tvtp"}, evaluated at the in-sample covariate means).}
\item{\code{correlations}}{Regime lower-triangular correlations (rows ordered by ascending mean).}
\item{\code{covariances}}{Array \eqn{K \times K \times N} of regime correlation matrices.}
\item{\code{log_likelihood}}{In-sample log-likelihood; full-sample when \code{out_of_sample = FALSE}.}
\item{\code{log_likelihood_oos}}{OOS log-likelihood on held-out 30 percent, or \code{NULL}.}
\item{\code{beta}}{TVTP coefficients (only for \code{"tvtp"}; \code{NULL} otherwise).}
\item{\code{par}, \code{coefficients}}{Named vector of estimated parameters in objective order.}
\item{\code{vcov}}{Observed-information variance-covariance at the MLE, or \code{NULL} if
the Hessian is singular/unavailable (e.g. at a bound).}
\item{\code{se}}{Named standard errors (\code{sqrt(diag(vcov))}), or \code{NULL}.}
\item{\code{convergence}}{\code{optim} convergence code (0 = converged).}
\item{\code{npar}, \code{nobs}}{Number of free parameters and observations used in estimation.}
\item{\code{method}, \code{N}, \code{K}, \code{p}, \code{call}}{Fit metadata.}
}
Standard methods are provided: \code{\link[=print.rsdc_fit]{print}}, \code{summary},
\code{coef}, \code{logLik} (so \code{\link[stats]{AIC}}/\code{\link[stats]{BIC}} work),
\code{nobs}, \code{vcov}, \code{confint}, \code{predict}, and \code{simulate}.
}
\description{
Unified front-end that dispatches to one of three estimators:
\itemize{
\item \code{f_optim()} — TVTP specification (\code{method = "tvtp"}).
\item \code{f_optim_noX()} — fixed transition matrix (\code{method = "noX"}).
\item \code{f_optim_const()} — constant correlation, single regime (\code{method = "const"}).
}
}
\details{
\itemize{
\item \strong{Method selection:} \code{match.arg()} validates \code{method}.
\item \strong{Inputs:} \code{"tvtp"} requires non-NULL \code{X}; \code{N} is ignored for \code{"const"}.
\item \strong{Split:} If \code{out_of_sample = TRUE}, the first 70\% is used for fitting.
}
}
\section{Functions}{
\itemize{
\item \code{print(rsdc_fit)}: Compact printer for a fitted model.

\item \code{coef(rsdc_fit)}: Coefficient vector (named, in objective order).

\item \code{nobs(rsdc_fit)}: Number of observations used in estimation.

\item \code{logLik(rsdc_fit)}: Log-likelihood (carries \code{df} and \code{nobs} so
\code{stats::AIC}/\code{BIC} work out of the box).

\item \code{vcov(rsdc_fit)}: Variance-covariance matrix of the estimates.

\item \code{confint(rsdc_fit)}: Wald confidence intervals from the observed information.

\item \code{summary(rsdc_fit)}: Summary with a coefficient table (estimate, SE, z, p).

\item \code{predict(rsdc_fit)}: Forecast from a fitted model (wraps
\code{\link{rsdc_forecast}}); supply the residuals, conditional volatilities,
and (for \code{"tvtp"}) the covariate matrix \code{X}.

\item \code{simulate(rsdc_fit)}: Simulate from a fitted model. For \code{"tvtp"} supply
a covariate matrix \code{X} (its row count sets the length); for \code{"noX"}
the fixed transition matrix is used; for \code{"const"} a single regime is drawn.

}}
\examples{
\donttest{
y <- scale(matrix(rnorm(100 * 3), 100, 3))
fit <- rsdc_estimate("noX", residuals = y, N = 2)
print(fit)
summary(fit)
c(AIC = AIC(fit), BIC = BIC(fit))
X <- cbind(1, scale(seq_len(nrow(y))))
fit_tvtp <- rsdc_estimate("tvtp", residuals = y, N = 2, X = X)
coef(fit_tvtp)
}

}
\references{
\insertRef{DEoptim-JSS}{RSDC} \cr

\insertRef{hamilton1989}{RSDC} \cr

\insertRef{pelletier2006regime}{RSDC}
}
\seealso{
\code{\link{rsdc_hamilton}} and \code{\link{rsdc_likelihood}}.
}
