# RcppArmadillo delegates dense linear algebra to the BLAS/LAPACK that R
# was built with; without this the Windows link fails with undefined
# references to dgemv_/dgemm_/dsyev_/dgetrf_ etc.
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
